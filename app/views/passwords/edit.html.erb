<%= turbo_frame_tag :modal do %>
  <%= render Shared::ModalComponent.new do %>
    <h1 class="font-bold text-4xl text-blue-800"><%= t('passwords.edit.title') %></h1>

    <%= turbo_frame_tag dom_id(@password, 'form') do %>
      <%= render partial: 'passwords/form', locals: { password: @password } %>
    <% end %>
  <% end %>
<% end %>
