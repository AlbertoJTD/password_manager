<div>
  <div class="flex items-center justify-between">
    <h1 class="font-bold text-4xl text-blue-800">All Passwords</h1>

    <div class="">
      <%= link_to 'New Password', new_password_path, class: 'bg-blue-500 text-white rounded-md px-4 py-2 hover:bg-blue-600', data: { turbo_frame: :modal } %>
    </div>
  </div>

  <div class="grid grid-cols-3 gap-4 mt-5">
    <%= render collection: @passwords, partial: 'password' %>
  </div>
</div>
