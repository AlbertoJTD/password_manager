<nav class="flex items-center justify-between mb-10">
  <div class="flex items-center gap-2">
    <%= link_to 'Password Manager', root_path, class: 'text-2xl font-bold hover:underline' %>
    <ul>
      <li><%= link_to 'Home', root_path, class: 'hover:underline' %></li>
    </ul>
  </div>

  <ul class="list-none flex gap-2">
    <% if user_signed_in? %>
      <li><%= link_to 'Profile', edit_user_registration_path, class: 'hover:underline'%></li>
      <li><%= link_to 'Logout', destroy_user_session_path, data: { turbo_method: :delete }, class: 'hover:underline'%></li>
    <% else %>
      <li><%= link_to 'Login', new_user_session_path, class: 'hover:underline'%></li>
      <li><%= link_to 'Sign up', new_user_registration_path, class: 'hover:underline'%></li>
    <% end %>
  </ul>
</nav>
